"""Notely automations: capability catalog, workflow model, engine, scheduler and AI planner.

The engine is app-agnostic. Every action — a Notely built-in, an AI step or a connector
tool — comes from the capability catalog, so adding a connector adds automation actions
without touching this package. See docs/automations.md.
"""
